
export const ENTRY_KEYWORDS_FOR_REPOS = [""] // these will be used to find initial repos. In the future I might find keywords to use 

export const ENTRY_KEYWORDS_FOR_COMMITS = [""] // these will be used to fetch initial commits

export const PER_PAGE_COMMITS = 1 // How many commits to fetch per page. Keeping low reduces chance of a rate limit.

export const PER_PAGE_REPOS = 1 // same as commits but for repos

export const PER_PAGE_CODE = 1 // same as commits but for code